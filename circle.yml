dependencies:
  override:
    - pip install -U docker-compose
    - echo SECRET_KEY=_ > configs/env/secret.env
    - docker-compose pull
machine:
  services:
    - docker
test:
  override:
    - docker-compose run web python manage.py collectstatic --noinput collectstatic
    - docker-compose run web python manage.py test
