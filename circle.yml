dependencies:
  pre:
    - pip install -U docker-compose
    - echo SECRET_KEY=_ > configs/env/secret.en
machine:
  services:
    - docker
test:
  override:
    - docker-compose run web python manage.py test
